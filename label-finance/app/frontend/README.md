# Frontend (interfaccia)

Qui andrà l'interfaccia del gestionale: caricamento file, dashboard, grafici,
tabelle di entrate/uscite. È la parte che l'utente vede e usa nel browser.

Ancora vuoto — si popola in Fase 1 (vedi `../../docs/PROGETTO.md`).
